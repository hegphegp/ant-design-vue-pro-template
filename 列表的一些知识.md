
```
1）列表查询条件的控件，必须放在<a-row>标签里面，且控件必须放在<a-col>这个标签里面，不放在<a-col>标签内排版会乱
2）覆盖官方样式，给<a-row> 添加 style="margin-left: -6px; margin-right: -6px;"，每个<a-col>标签添加 style="padding-left: 6px; padding-right: 6px;"
3）去掉搜索按钮的外层嵌套标签，不能嵌套<span>标签，按钮数量过多时，右侧的按钮会被隐藏掉
4）把 src/global.less 文件中的 table-page-search-wrapper -> ant-form-inline -> ant-form-item-label 的 padding-right 改成 0px，应该可以直接删除这一行 padding-right 的配置
5）把 src/global.less 文件中的 table-page-search-wrapper -> ant-form-inline -> ant-form-item 的 margin-bottom 改成 8px
5）把 src/global.less 文件中的 table-page-search-submitButtons 的 margin-bottom 改成 8px
6）最简单的点击 查询按钮 触发列表查询的方法 <a-button style="margin-bottom: 8px; margin-left: 8px" type="primary" @click="$refs.table.refresh(true)">查询</a-button>
    `this.$refs.table.refresh(true)` 刷新列表 (用户新增/修改数据后，重载列表数据)
7）最简单的点击 重置按钮 重置列表查询参数的方法 <a-button style="margin-bottom: 8px; margin-left: 8px" @click="() => this.queryParam = {}">重置</a-button>
8）获取所有查询的最简单的方法是 在loadData方法用 const requestParameters = Object.assign({}, parameter, this.queryParam) 获取
9）删除 src/components/Table/index.js 的 311 行 { showAlert ? this.renderAlert() : null }
```

#### 修改table页码的范围
```
<s-table
  ref="table"
  size="default"
  :pagination="pagination">
</s-table>
export default {
  data () {
    return {
      pagination: {
        showTotal: total => `共 ${total} 条数据`,
        pageSizeOptions: ['10', '20', '50', '100']
      }
    }
  }
}
```